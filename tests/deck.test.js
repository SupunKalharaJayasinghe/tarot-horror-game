// Placeholder test for Deck — set up your test runner (e.g., vitest, jest) later.
// Example (pseudo):
// import { describe, it, expect } from 'vitest';
// import { createDeck } from '../src/components/Deck.js';
//
// describe('Deck', () => {
//   it('creates deterministic order with same seed', async () => {
//     const d1 = await createDeck(123);
//     const d2 = await createDeck(123);
//     const a = Array.from({ length: 5 }, () => d1.draw().id).join(',');
//     const b = Array.from({ length: 5 }, () => d2.draw().id).join(',');
//     expect(a).toBe(b);
//   });
// });
